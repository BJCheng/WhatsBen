const recipientHeaderInitialState = {
  userId: '',
  firstName: '',
  lastName: '',
  thumbnailUrl: '',
  timestamp: ''
};
export default (state = recipientHeaderInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};