const inputFooterInitialState = {
  text: ''
};
export default (state = inputFooterInitialState, action) => {
  switch (action.type) {
    case 'CLICK':
      return state;
    default:
      return state;
  }
};